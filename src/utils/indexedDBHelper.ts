// import { get, set } from 'idb-keyval';

// export async function saveDB(db: any) {
//    const buffer = await db.exportFile();
//    await set('duckdb', buffer);
// }

// export async function restoreDB(db: any) {
//    const buffer = await get('duckdb');
//    if (buffer) {
//       await db.importFile(buffer);
//    }
// }
